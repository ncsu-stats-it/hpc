#!/bin/sh
##
# R -e "update.packages(ask=FALSE, dependencies=c('Suggests'), repos='https://cran.rstudio.com/')"
R -e "update.packages(
      lib.loc = \"$R_LIBS_USER\",
      repos='https://cran.rstudio.com/',
      ask=FALSE, 
      dependencies=c('Suggests'),
)"
# update.packages(ask=FALSE, dependencies=c('Suggests'), repos='https://cran.rstudio.com/')"
